---
# tasks file for IaC/k8s/tf/roles/common waitForSshConnection
- name: Sleep for 120 seconds and continue with play
  ansible.builtin.wait_for:
    timeout: 120
  delegate_to: localhost

- name: Wait for port 22 to become open
  ansible.builtin.wait_for:
    port: 22
